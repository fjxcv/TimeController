<UserControl x:Class="TimeController.Views.StrongGoalWeek.DateColumnControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			  xmlns:converters="clr-namespace:TimeController.Converters"
             mc:Ignorable="d">
	<Border Background="#F0F0F0" CornerRadius="4" Margin="2" MinHeight="100">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<!-- 日期标题 -->
			<StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
				<TextBlock Text="{Binding WeekDayText}" HorizontalAlignment="Center"/>
				<TextBlock x:Name="DateTextBlock" Text="{Binding DateText}" 
                           HorizontalAlignment="Center" FontSize="16" FontWeight="Bold"
                           Foreground="{Binding IsCurrentMonth, Converter={StaticResource BoolToColorConverter}}"/>
			</StackPanel>

			<!-- 全天任务列表 -->
			<ItemsControl Grid.Row="1" Margin="2,2,2,2" 
                          ItemsSource="{Binding AllDayTasks}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Vertical"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Grid MouseEnter="AllDayGrid_MouseEnter" MouseLeave="AllDayGrid_MouseLeave" Tag="{Binding}">
							<Border Background="{Binding Brush}" 
                                    CornerRadius="6" 
                                    Margin="2"
                                    Padding="6,2"
                                    MouseLeftButtonDown="TaskBlock_MouseLeftButtonDown">
								<Grid>
									<TextBlock Text="{Binding Name}" 
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Note}" />
									<!-- 删除按钮（叉号） -->
									<Button x:Name="DeleteButton"
                                            Content="✕" 
                                            Width="13" Height="13" 
                                            HorizontalAlignment="Right" 
                                            VerticalAlignment="Center" 
                                            Margin="1"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="Red"
                                            FontWeight="Bold"
                                            Tag="{Binding}"
                                            Click="DeleteAllDayButton_Click">
										<Button.Style>
											<Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
												<Setter Property="Visibility" Value="Collapsed"/>
												<Style.Triggers>
													<DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}}" Value="True">
														<Setter Property="Visibility" Value="Visible"/>
													</DataTrigger>
												</Style.Triggers>
											</Style>
										</Button.Style>
									</Button>
								</Grid>
							</Border>
						</Grid>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
				<ItemsControl.ItemContainerStyle>
					<Style TargetType="ContentPresenter">
						<Setter Property="Visibility" Value="Visible"/>
						<Style.Triggers>
							<!-- 显示该列的任务 -->
							<MultiDataTrigger>
								<MultiDataTrigger.Conditions>
									<Condition Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource IndexConverter}}" Value="0"/>
								</MultiDataTrigger.Conditions>
								<Setter Property="Visibility" Value="Visible"/>
							</MultiDataTrigger>
							<!-- 如果是第三个以上的任务，只有在展开状态下显示 -->
							<MultiDataTrigger>
								<MultiDataTrigger.Conditions>
									<Condition Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}, 
                                  Path=ItemsSource, Converter={StaticResource IndexConverter}, ConverterParameter=gt2}" Value="True"/>
									<Condition Binding="{Binding DataContext.IsExpanded, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}" Value="False"/>
								</MultiDataTrigger.Conditions>
								<Setter Property="Visibility" Value="Collapsed"/>
							</MultiDataTrigger>
						</Style.Triggers>
					</Style>
				</ItemsControl.ItemContainerStyle>
			</ItemsControl>

			<!-- 展开/折叠按钮 -->
			<Button Grid.Row="1" HorizontalAlignment="Center"
					VerticalAlignment="Bottom"
					Margin="0,2,0,0"
					Command="{Binding DataContext.ToggleColumnExpandCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
					CommandParameter="{Binding Index}"
					Visibility="{Binding ShouldShowMoreButton, Converter={StaticResource BoolToVisibility}}">
				<Button.Content>
					<TextBlock Text="{Binding IsExpanded, Converter={StaticResource ExpandedToSymbolConverter}}" />
				</Button.Content>
				<Button.Style>
					<Style TargetType="Button">
						<Setter Property="Background" Value="Transparent"/>
						<Setter Property="BorderThickness" Value="0"/>
						<Setter Property="Padding" Value="4,0"/>
						<Setter Property="FontSize" Value="12"/>
					</Style>
				</Button.Style>
			</Button>
		</Grid>
	</Border>
</UserControl>
