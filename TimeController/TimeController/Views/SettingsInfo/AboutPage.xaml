<ui:Page x:Class="TimeController.Views.SettingsInfo.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TimeController.Views.SettingsInfo"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AboutPage">

    <ScrollViewer  Padding="16">
        <StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="关于 TimeController"
                   FontSize="24"
                   FontWeight="Bold"
                   VerticalAlignment="Center" Margin="0,0,0,20"/>
            </StackPanel>
            
            <!-- 新手教程 -->
            <ui:SettingsCard Header="新手教程" Description="快速上手指南">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Glyph="&#xE8A5;" FontSize="20"/>
                </ui:SettingsCard.HeaderIcon>
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding TutorialSteps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="•" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:SettingsCard>

            <!-- 系统理念 -->
            <ui:SettingsCard Header="系统理念" Description="背后的设计哲学">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Glyph="&#xE83B;" FontSize="20"/>
                </ui:SettingsCard.HeaderIcon>
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding PhilosophyLines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="•" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:SettingsCard>

            <!-- 致谢 -->
            <ui:SettingsCard Header="致谢" Description="感谢以下贡献者与开源项目">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Glyph="&#xE716;" FontSize="20"/>
                </ui:SettingsCard.HeaderIcon>
                <ItemsControl ItemsSource="{Binding Acknowledgements}" Margin="0,8,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="•"
                                            Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding}"
                                            TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ui:SettingsCard>

        </StackPanel>
    </ScrollViewer>

</ui:Page>